## vscode-eslint
ESLint 是一个专门检查 JavaScript / TypeScript 代码问题的工具，可以把它理解成一个自动化的代码审阅员。
## esbuild-problem-matchers
让 VS Code 的错误面板能准确、美观读取和显示 `esbuild` 的编译错误和警告。
## extension-test-runner
提供一个现代化的、内置于 VS Code 的图形化界面，让插件的测试执行和调试像运行普通代码一样方便。